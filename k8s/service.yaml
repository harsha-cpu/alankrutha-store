apiVersion: v1
kind: Service
metadata:
  name: alankrutha
  namespace: alankrutha
spec:
  selector:
    app: alankrutha
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP