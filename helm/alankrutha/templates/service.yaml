apiVersion: v1
kind: Service
metadata:
  name: {{ include "alankrutha.fullname" . }}
  labels:
    {{- include "alankrutha.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "alankrutha.selectorLabels" . | nindent 4 }}
